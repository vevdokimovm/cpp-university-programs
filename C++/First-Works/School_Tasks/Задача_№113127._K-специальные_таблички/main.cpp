#include <iostream>

using namespace std;

int main()
{
    int b, g;
    cin >> b >> g;
    if ((double)b / g < 0.7 || (double)g / b < 0.7)
        cout << "NO SOLUTION"
        return 0;
    else {

    }
    return 0;
}
