#include <iostream>

using namespace std;


string permutations(string str){
    if (str.size() == 1)
        return str;
    else
}

int main()
{
    string str = "";
    cin >> str;
    permutations(str);
    return 0;
}
