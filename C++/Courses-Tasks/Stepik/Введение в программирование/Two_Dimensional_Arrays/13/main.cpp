#include <iostream>

using namespace std;

int main()
{
    unsigned counter = 1, rows = 0, cols = 0;
    cin >> rows >> cols;
    unsigned a[rows][cols];
    bool flag = true; // true - row, false - col
    for (size_t i = 0; i )
}
