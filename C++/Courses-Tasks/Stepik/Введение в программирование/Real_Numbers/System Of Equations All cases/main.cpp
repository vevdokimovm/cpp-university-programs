#include <iostream>

using namespace std;

int main()
{
    double a, b, c, d, e, f, x, y, D;
    cin >> a >> b >> c >> d >> e >> f;
    if (a == 0 && b == 0 && c == 0 && d == 0 && e == 0 && f == 0)
    {
        cout << 5;
    }
    else
    cout << "Hello world!" << endl;
    return 0;
}
